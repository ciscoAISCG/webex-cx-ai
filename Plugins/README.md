# Plugins

Installable AI SCG plugin packages will be published here.

No plugins have been published yet.

Each future plugin will use its own folder:

```text
Plugins/
└── <plugin-name>/
```

Plugin contributions must use a feature branch and pull request. Do not include
credentials, customer data, or other private information.
